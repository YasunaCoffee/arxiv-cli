name: Daily AI Character Search

on:
  schedule:
    - cron: '0 3 * * *'  # 毎日 JST 12:00
  workflow_dispatch:

jobs:
  search:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Checkout arxiv-data
        uses: actions/checkout@v4
        with:
          repository: YasunaCoffee/arxiv-data
          token: ${{ github.token }}
          path: arxiv-data

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install arx
        run: pip install -e .

      - name: Fetch AI character papers
        run: python scripts/fetch_ai_character.py > /tmp/ai_papers.json
        env:
          ARX_DB_PATH: ./arxiv-data/papers.db

      - name: Notify Discord
        run: python scripts/notify_discord.py < /tmp/ai_papers.json
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Push updated DB to arxiv-data
        run: |
          cd arxiv-data
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add papers.db
          git diff --cached --quiet || (git commit -m "chore: ai-character fetch $(date +%Y-%m-%d)" && git push)
